# Configuración del servidor
PORT=3000
NODE_ENV=development

# Base de datos PostgreSQL
# Para desarrollo local, usa: postgresql://usuario:contraseña@localhost:5432/nombre_db
# Para Render, este valor se genera automáticamente
DATABASE_URL=postgresql://localhost:5432/donation_db

# JWT Secret (¡Cambia esto en producción!)
JWT_SECRET=tu-secreto-super-seguro-cambialo-en-produccion

# Ejemplo para producción en Render:
# DATABASE_URL se configura automáticamente
# NODE_ENV=production
# JWT_SECRET=genera-un-secreto-aleatorio-seguro